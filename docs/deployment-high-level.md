# Deployment (High Level)

Petbae is deployed across multiple environments (development, staging, and
production) with controlled access and strict separation.

This repository does not include deployment pipelines, infrastructure code,
or environment-specific configuration.

